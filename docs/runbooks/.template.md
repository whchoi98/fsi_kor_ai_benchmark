# Runbook — <incident or operation name>

- **Owner**: <team / role>
- **Severity**: SEV-1 | SEV-2 | SEV-3 | Routine
- **Last verified**: YYYY-MM-DD

## When to use this runbook

Symptoms / triggers that should send an operator here.
(e.g. "`run_benchmark.sh` exits with `ThrottlingException` more than 3 times in 60s")

## Pre-checks (do not skip)

- [ ] Confirm `AWS_BEARER_TOKEN_BEDROCK` or IAM creds are valid: `aws sts get-caller-identity`
- [ ] Confirm region availability: `aws bedrock list-foundation-models --region $AWS_REGION`
- [ ] Confirm `output/` is writable and has space for ~10 MB of artifacts.
- [ ] Confirm there isn't an active run still writing `*.progress.jsonl` (check `ps`, file mtime).

## Diagnosis

Steps to confirm root cause. Each step should produce a yes/no answer.

1. ...
2. ...

## Mitigation

Numbered steps with exact commands. Include rollback for any destructive step.

```bash
# example
./run_benchmark.sh --resume    # resume without --reset
```

## Verification

How to confirm the issue is resolved.

- [ ] `validate_side()` passes (run with `--report`)
- [ ] All 300 indices present in both deliverable JSONLs
- [ ] `comparison_report.md` regenerated cleanly

## Post-incident

- [ ] Root cause noted in `output/runner.log` or attached to incident ticket.
- [ ] If Bedrock-side issue: open AWS support case, capture request IDs.
- [ ] If recurring: open ADR or update this runbook.
